describe('FileManager', function () {
    var fm;

    beforeEach(function () {
        var sd_stub = {};
        fm = new FileManager(sd_stub);
    });
    
    // I have no idea how to test this...
});